function preFizz(n) {
  var arr = [];
  var num = 1;
  if (num === n) {
    arr.push(num);
    return arr;
  } else if (num !== n) {
    num = n + 1;
    for (var j = 1; i < arr.length; i++) {
      if (num ===)
    }
  }
}
